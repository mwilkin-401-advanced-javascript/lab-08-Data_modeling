'use strict';

const uuid = require('uuid/v4');

const schema = {
};

class Products {

  constructor() {
    this.database = [];
    this.product = product;
  }

  get(id) {
  }
  
  post(entry) {
  }

  put(id, entry) {
  }

  delete(id) {
  }

  sanitize(entry) {
  }

}

module.exports = Products;
